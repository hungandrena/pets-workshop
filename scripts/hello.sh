#!/usr/bin/env bash
#
# Prints a greeting. Framework-free on purpose: no toolchain beyond bash, so it
# runs the same on a developer machine and in CI.

set -euo pipefail

echo "Hello from the pets workshop!"
